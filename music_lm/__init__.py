"""Small GPT-style music language model utilities for PianoMime.

Submodules intentionally own their heavier imports so that training utilities can
be inspected even before optional MIDI parsing dependencies are installed.
"""
